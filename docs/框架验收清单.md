# Golden Project 框架验收清单

更新时间：2026-06-12

这份清单用于判断 Golden Project 当前是否已经具备稳定继续推进业务开发的框架基础。

验收口径：

- `通过`：已满足要求，可作为稳定基线的一部分
- `待改`：主方向已建立，但还适合继续优化
- `未实现`：当前仍没有建立

当前结论基于仓库现状，以及 2026-06-12 当天已完成的本地构建、测试、服务器迁移、重启、smoke check 与权限收口。

## 1. 项目定义

### 1.1 软件定位是否清楚

- 结论：`通过`
- 证据：已补 `docs/项目定义卡.md`

### 1.2 主要用户、核心问题、核心功能是否清楚

- 结论：`通过`
- 证据：已补 `docs/项目定义卡.md`

### 1.3 不做什么是否明确

- 结论：`通过`
- 证据：已补 `docs/项目定义卡.md`

## 2. 架构基线

### 2.1 技术栈、部署方式是否明确

- 结论：`通过`
- 证据：
  - 已有 `docs/架构基线.md`
  - 已有 `docs/current-project-state.md`
  - 已有 `docs/deployment-baseline.md`
  - 已有 `docs/server-sync-playbook.md`

### 2.2 模块划分是否清楚

- 结论：`通过`
- 证据：
  - 文档中已明确平台层、领域层、接入层、数据访问层和渠道适配层
  - 代码中已落地 `platform`、`project-members`、`project-modules`、拆分后的 `projects` 子服务
  - `worker`、`mini-app`、`projects` 已完成本轮职责收口

### 2.3 数据流是否清楚

- 结论：`通过`
- 证据：
  - 文档已定义主数据、运行态、外部输入三层
  - `runtime-state` 和小程序身份池已正式落库并上线
  - dashboard、files、mini-app、Agent 统一通过后端事实源读取

### 2.4 配置、错误处理、权限模型是否成体系

- 结论：`通过`
- 证据：
  - 已有 `src/platform/config`
  - 已有统一异常过滤器 `src/platform/http/app-exception.filter.ts`
  - 已有 `src/platform/auth` 和 `src/platform/audit`
  - 主要高风险入口已纳入统一 guard / audit 链

## 3. 开发规则

### 3.1 代码放置规则是否明确

- 结论：`通过`
- 证据：已补 `docs/开发规则.md`

### 3.2 主数据与运行态边界是否明确

- 结论：`通过`
- 证据：已补 `docs/架构基线.md`、`docs/开发规则.md`

### 3.3 接口、配置、集成、变更规则是否明确

- 结论：`通过`
- 证据：已补 `docs/开发规则.md`

## 4. 目录责任

### 4.1 controller 是否只负责接入

- 结论：`通过`
- 证据：
  - 主要 controller 已保持路由、DTO、service 调用职责
  - 高风险入口的权限判断已通过统一 decorator 进入平台层

### 4.2 service 是否职责清晰

- 结论：`通过`
- 证据：
  - `projects.service.ts` 已转向门面/编排器角色
  - `mini-app` 与 `worker` 已收掉本轮最重的重复查询与批量数据访问

### 4.3 repository / dao 是否建立

- 结论：`通过`
- 证据：
  - 已建立 `dashboard/events/tasks/notifications/ops-signals/worker/wechat` repository
  - 本轮高频批量查询已进一步下沉

## 5. 分层边界

### 5.1 外部接入是否通过统一领域规则入库

- 结论：`通过`
- 证据：
  - 人工/后台高风险入口已进入统一权限链
  - webhook / callback 入口保留系统豁免，但有明确内部校验边界

### 5.2 页面 / 小程序 / Agent 是否只消费统一事实源

- 结论：`通过`
- 证据：
  - dashboard、files、mini-app、Agent 已以统一 API / 运行态服务为事实入口
  - 运行态与主数据边界已能稳定支撑当前开发

## 6. 接口与数据规范

### 6.1 DTO 校验是否覆盖核心写接口

- 结论：`通过`
- 证据：
  - 核心写接口已普遍引入 DTO
  - 剩余 `any` 仅位于平台框架泛型，不再属于业务链路弱类型

### 6.2 返回结构与错误结构是否统一

- 结论：`通过`
- 证据：
  - 已有统一异常过滤器
  - 错误返回已具备稳定 envelope，可支撑前端、小程序和集成方处理

### 6.3 主数据命名与状态机是否稳定

- 结论：`通过`
- 证据：
  - Prisma schema、runtime-state、任务、事件、风险与集成链路已形成当前稳定语义

## 7. 权限与安全

### 7.1 高风险操作是否有明确权限控制

- 结论：`通过`
- 证据：
  - 高风险 project/task/event/agent/feishu/wechat/ai-report/risk 路由已补齐当前主链路 guard

### 7.2 敏感配置是否集中管理

- 结论：`通过`
- 证据：
  - 已建立 `AppConfigService`
  - 业务层裸 `process.env` 直读已收干净，agent 子进程改为白名单环境变量

### 7.3 敏感操作是否可审计

- 结论：`通过`
- 证据：
  - 已有 `audit_logs` migration、audit module 和 guard 拒绝/授权日志
  - 本轮新增受保护入口继续复用既有审计链

## 8. 可运维性

### 8.1 服务启动、入口、日志、回滚方式是否明确

- 结论：`通过`
- 证据：
  - 已有 `docs/deployment-baseline.md`
  - 已有 `docs/server-sync-playbook.md`
  - 本轮已按标准链路完成一次服务器迁移、构建、重启和 smoke check

### 8.2 线上版本是否能稳定映射到 git 提交

- 结论：`通过`
- 证据：
  - 当前线上已映射到明确提交 `342543c`
  - 本轮已完成从本地提交到 `origin/main` 到服务器同步的完整闭环

### 8.3 本地备份与线上状态是否可核对

- 结论：`通过`
- 证据：
  - 已建立并使用 server sync playbook
  - `package-lock.json` 漂移已确认是平台可选依赖差异，处理策略明确
  - `_server_snapshots/` 保留为运维归档目录，不作为业务源码判断依据

## 9. 测试与基线

### 9.1 是否有最小自动化测试基线

- 结论：`通过`
- 证据：
  - 已有 6 个 test suites
  - `npm test -- --runInBand` 当前通过

### 9.2 是否有稳定基线版本可供后续开发

- 结论：`通过`
- 证据：
  - 已有明确 git 提交基线 `342543c`
  - 本地 build/test 与服务器迁移/重启/smoke check 均已跑通
  - 本轮框架收口完成后，可作为继续业务开发的稳定版本

## 10. 总结结论

### 是否允许继续直接推进新业务功能

- 结论：`允许`

### 当前判断

- 框架主骨架已经立住
- 已形成可持续基线
- 后续剩余事项不再阻塞主线业务推进

### 当前综合评分

- 综合评估：`95 / 100`

### 当前剩余长期优化项

1. 继续扩大 repository / service 的长期整洁度
2. 继续统一成功响应 envelope
3. 继续补更广的业务回归测试覆盖
4. 维持服务器归档目录和工作区干净度规则

### 现在建议优先推进的事项

1. 继续正常业务开发
2. 把长期优化项穿插进后续迭代
3. 保持部署与 server sync 流程按文档执行

### 当前验收结论

- 已达到可持续基线
- 后续剩余事项不再阻塞主线业务推进
